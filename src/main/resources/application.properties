spring.application.name=MarketList

# Config do PostgreSQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:PAPtOZIahdxxDmcncHUnaKmnMDqcMNzZ}

# Config do Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Server
server.port=${PORT:8080}
spring.web.resources.add-mappings=true

# memory config
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hiraki.minimum-idle=2
spring.datasource.hiraki.idle-timeout=30000
spring.datasource.hiraki.max-lifetime=60000